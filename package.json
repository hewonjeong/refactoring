{
  "name": "refactoring",
  "version": "0.0.1",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "parcel index.html --port=3000 && tsc --noEmit --watch",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.13",
    "jest": "^26.4.2",
    "parcel-bundler": "^1.12.4",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  },
  "jest": {
    "preset": "ts-jest"
  }
}
